#!/usr/bin/env python3
"""Verify recorded envelopes and render the evidence report without contacting providers."""
import json,pathlib,hashlib,plistlib,os,subprocess,signal,time
root=pathlib.Path(__file__).resolve().parents[2];a=root/'artifacts'
def load(name):return json.loads((a/name).read_text())
r=load('pinned-runtime-raw-0.16.3.json');u=load('pinned-runtime-complete-read-0.16.3.json');p=load('pinned-runtime-pagination-0.16.3.json');f=load('pinned-runtime-failure-cursor-0.16.3.json');b=load('session-channel-economics-baseline.json')
def clirows(records,verb):return [json.loads(x['stdout']) for x in records if 'argv' in x and len(x['argv'])>2 and x['argv'][2]==verb and x.get('stdout','').startswith('{')]
status=clirows(u,'status')[0]['result']['status'];items=next(x['channelResponse']['page']['items'] for x in u if x.get('channelRequest',{}).get('query')=='transcript.list' and x['channelResponse'].get('ok'))
assistant=next(x for x in items if x.get('role')=='assistant');prefix=assistant['body'].encode().startswith(status['content']['text'].encode())
rawfile=next((pathlib.Path(u[0]['temporaryRoot'])/'agent/sessions').rglob('*.jsonl'));entries=[json.loads(l) for l in rawfile.read_text().splitlines()];byid={x['id']:x for x in entries if 'id' in x};ids=[x['id'] for x in items]
append_ids=[x['id'] for x in entries if x.get('type')=='message' and x['id'] in ids];ancestry=append_ids==ids
for before,after in zip(ids,ids[1:]):
    parent=byid[after].get('parentId');seen=set()
    while parent and parent!=before and parent not in seen:seen.add(parent);parent=byid[parent].get('parentId')
    ancestry=ancestry and parent==before
pages=[]
for x in p:
    if 'pageCount' in x:break
    if x.get('channelRequest',{}).get('query')=='transcript.list' and x['channelResponse'].get('ok'):pages.append(x['channelResponse']['page'])
allitems=[item for page in pages for item in page['items']]
parts=[x['channelResponse']['page']['items'][0] for x in p if x.get('channelRequest',{}).get('query')=='Q23' and x['channelResponse'].get('ok')]
body=''.join(x['body'] for x in parts if x['field']=='body');expected='한'*100000
assert len(pages)>=50 and pages[-1]['complete'] and len(allitems)==520
assert [x['id'] for x in allitems]==[f'{i:08x}' for i in range(520)]
assert body.encode()==expected.encode();assert prefix and ancestry and status['content']['truncated']
assert all(json.loads(t['stdout'])['result']['status']=='terminal_ok' for cohort in b['K'].values() for t in cohort['turns'])
assert set(b['K'])=={'1','2','4'}
verification={'unicodeContentBytes':len(status['content']['text'].encode()),'unicodeAssistantBytes':len(assistant['body'].encode()),'exactBytePrefix':prefix,'projectionMatchesAppendAndParentAncestry':ancestry,'ancestryRows':[{k:byid[i][k] for k in ['id','parentId','timestamp']} for i in ids],'paginationPages':len(pages),'paginationItems':len(allitems),'oversizedBodyBytes':len(body.encode()),'oversizedBodySha256':hashlib.sha256(body.encode()).hexdigest(),'all520IdsInAppendOrder':True,'all30EconomicsTurnsTerminalOk':True}
(a/'pinned-runtime-verification-0.16.3.json').write_text(json.dumps(verification,ensure_ascii=False,indent=2))
lines=['# Pinned runtime recordings — gjc/0.16.3','', '**I4a decision: path (ii), channel query (`session.checkpoint` → checkpointToken-seeded `transcript.list` → same-connection continuation + Q23). Path (i), CLI one-shot complete read, FAILS with `uncertain_after_send`.**','', '**Gates: C1 PASS; C2 PASS; C3 PASS; C4 PASS; C5 PASS. W1 permits channel cutover consideration; this is not an I9 deployment approval. I4b case (2): ENABLED for the channel path (snapshot identity, byte prefix, and append/parent ancestry verified).**','', '## Provenance and procedure', '',f"- Realpath: `{r[0]['realpath']}`",f"- SHA-256: `{r[0]['sha256']}`", '- Version envelope: `gjc/0.16.3`.', '- All runtime calls used this executable, temporary workspace and GJC_CODING_AGENT_DIR under $TMPDIR. Seeded models.yml, model-presets/, config.yml from operator SSOT; launchd provider credentials were loaded only in memory. No live deployment or source changes.', '- Exact creation argv discovered from help: `gjc sdk session raw global --op session.create --idempotency-key <key> --json-input {"cwd":"<temporary-workspace>"}`. No `session create` verb exists.', '- Channel controls use the frame field `operation`, not `op`. Queries use `query`. Model profile `gpt-default`; explicit `layofflabs/gpt-5.4-mini` for Unicode. Failed turn uses gpt-image-2 with retries disabled only in throwaway config.', '- Economics: sequential isolated K=1,2,4 cohorts, 300 seconds idle per K (parent-authorized reduction from requested 600), then 10 real terminal_ok turns per K. Reproduce full requested window with `python3 scripts/recordings/probe.py --idle-seconds 600`. RSS/threads from Darwin PROC_PIDTASKINFO; actual FD count from returned PROC_PIDLISTFDS buffer bytes / 8. Percentiles are nearest-rank; aggregate p95 uses each simultaneous sample sum, not sum of p95s. Cold-start distributions have K observations each; small samples are explicitly not statistically robust.', '- An initial run was interrupted by parent SIGTERM; a second baseline used an FD capacity estimate. Both were discarded; delivered baseline reran all K with actual FD counts. The first unspecified-model long prompt hung; explicit model selection produced the successful Unicode sample.', '', '## Capability verdicts', '', '- C1 PASS — control_request turn.prompt returns accepted receipt with matching clientRef.', '- C2 PASS — query_request turn.result kind=prompt/clientRef resolves that receipt (accepted status observed).', '- C3 PASS — query_request session.checkpoint returns signed checkpointToken and revisionId.', '- C4 PASS — 60 transcript pages, all 520 IDs in append order; item 00000082 is oversized and Q23 reconstructs its exact 300000-byte Unicode body on one connection.', '- C5 PASS — turn.steer, model.profile.set, service_tier.set and queue.messages.list each returned ok:true; queued steer row observed.', '', '## Session discovery and OS identity', '', '- list/inspect return `locator.cwd`, `locator.worktreeRoot` (null outside git), `locator.stateRoot`, pid, lastHeartbeatAt. No repo field. **This pinned executable omits processIncarnation in these rows and emits hostIncarnation instead**; do not claim the missing field exists.', '- Source inspected: `/Users/bellman/Documents/Workspace/gajae-code/packages/coding-agent/src/sdk/broker/process-incarnation.ts:101-110,124-157`. Darwin identity is `darwin:<start-seconds>:<start-microseconds>` from native Process.fromPid / proc_pidinfo BSDINFO, not a random UUID. PID plus this OS start stamp can be compared; the row field is hostIncarnation. Source checkout is explanatory evidence, not proof that its current commit equals the compiled binary.', '', '## Unicode, snapshot, and projection correlation', '', '```json',json.dumps(verification,ensure_ascii=False,indent=2),'```', '', '- Full transcript projection omits parentId, so ancestry was checked against the same private saved JSONL. The two sampled message IDs occur in append order and the assistant parent points to the user row.', '- Channel checkpoint revisionId is the authoritative snapshot revision; all 60 checkpoint-seeded pages report revision 1 in the pagination sample. Checkpoint high watermark is revision 520/generation 1. CLI tail did not deliver a complete result.checkpoint, so no tail snapshot identity is claimed.', '', '## Cursor lifecycle', '', '- Fresh checkpoint exchange on another serve connection: PASS. Fresh channel token exchanged through a new CLI raw-query connection using a 0600 JSON input file: PASS. CLI output intentionally strips checkpointToken; it retains revisionId/checkpoint/issuedAt/expiresAt.', '- Foreign string and old-format envelope: observed invalid_cursor.', '- Transcript continuation on another connection: observed cursor_expired, even when fresh. This is connection authority failure, not elapsed TTL. Trying a foreign connection releases the token, so the same original token subsequently fails on its original connection too.', '- Exchanging a checkpoint consumes/transfers its original pin. Reusing that original after exchange produces cursor_expired. The successful complete read does not do this.', '- CLI tail with the fresh signed token observed invalid_cursor; bare tail with `--all-events --timeout-ms 1` observed uncertain_after_send on both tiny and 60-page transcripts. It returns no full item bodies, so path (i) fails.', '- Fifteen-minute expiry is SOURCE-DERIVED, not a real 15-minute wait: `query/revision-store.ts:35` defines SNAPSHOT_TTL_MS = 15*60*1000; `query/cursor.ts:4,159,240-251` binds expiry and checks it. Envelope expiresAt-issuedAt is observed as 900000ms. No supported CLI/env TTL override was found; no modified binary/source test is presented as a pinned-runtime observation.', '', '## Large transcript qualification', '', '- Stress transcript is explicitly synthetic: a real private session was created, its host stopped and verified absent, then its private saved v5 JSONL replaced offline with 520 parent-linked user rows (519 × roughly 13KB text; one × 300KB Unicode). The pinned runtime resumed the canonical realpath and served all 60 pages. This tests read transport, not 520 model-generated turns.', '- Q23 oversized body chunks: byte offsets 0 and 262143, lengths 262143 and 37857, complete false then true. Content JSON field is independently continued. Raw envelopes preserve all full bodies in the pagination JSON.', '', '## Economics summary', '', '| K | Samples | Aggregate RSS p95 bytes | FDs p95 | Threads p95 | Cold p50 ms | Cold p95 ms |', '|---|---:|---:|---:|---:|---:|---:|']
for k,v in b['K'].items():
    z=v['aggregateP95'];lines.append(f"| {k} | {v['sampleCount']} | {z['rssBytes']} | {z['fds']} | {z['threads']} | {v['coldStartP50Ms']:.1f} | {v['coldStartP95Ms']:.1f} |")
lines+=['', '## Raw envelopes', '', 'The JSON artifacts retain exact full stdout and channel response envelopes (provider secrets redacted). Array indices below are zero-based. JSONL ancestry receipt is above. Large bodies are retained in the raw JSON rather than duplicated throughout this document.']
for name,records in [('pinned-runtime-raw-0.16.3.json',r),('pinned-runtime-complete-read-0.16.3.json',u),('pinned-runtime-failure-cursor-0.16.3.json',f),('pinned-runtime-pagination-0.16.3.json',p)]:
    lines+=['',f'### `{name}`','']
    for i,x in enumerate(records):
        if name.endswith('pagination-0.16.3.json') and 'channelRequest' in x:
            response=x['channelResponse'];page=response.get('page',{})
            if page.get('items') and not any(t.get('error') for t in page['items']):
                lines.append(f'- Raw envelope [{i}]: `{x["channelRequest"].get("query")}`; ok={response.get("ok")}; page revision={page.get("revision")}; items={len(page["items"])}; complete={page.get("complete")}. Full bodies: `{name}` array index {i}.');continue
        if 'argv' in x and len(x['argv'])>2 and x['argv'][2]=='send' and name.startswith('pinned-runtime-raw'):continue
        y=x.get('channelResponse',x)
        if 'stdout' in x:
            try:y=json.loads(x['stdout'])
            except:y={'stdout':x['stdout'],'exitCode':x.get('exitCode')}
        lines += [f'Raw envelope [{i}]:','```json',json.dumps(y,ensure_ascii=False,indent=2),'```']
lines+=['','## Limitations and unobserved items','','- 600-second idle windows were reduced to 300 seconds by explicit parent authorization; rerun with 600 for exact original acceptance. No claim of 10-minute observations is made.','- Real wall-clock 15-minute cursor expiry not waited; source-derived only.','- Tail complete result/checkpoint not observable because one-shot CLI fails before returning it.','- Discovery row processIncarnation is absent; hostIncarnation observed instead.','- 60-page workload is synthetic saved transcript, not a real 520-turn provider conversation.','- Initial failures and invalid harness frames were diagnosed and rerun; only corrected W1 envelopes determine PASS.','- No full project suite/lint/typecheck/format run (parent owns final verification).']
(a/'pinned-runtime-recordings-0.16.3.md').write_text('\n'.join(lines)+'\n')
secrets=plistlib.loads((pathlib.Path.home()/'Library/LaunchAgents/dev.gajaeway.gateway.plist').read_bytes()).get('EnvironmentVariables',{})
for path in list(a.glob('pinned-runtime-*0.16.3*'))+[a/'session-channel-economics-baseline.json']:
    data=path.read_text()
    for k,v in secrets.items():
        if any(t in k for t in ['KEY','TOKEN','SECRET']) and isinstance(v,str) and v:assert v not in data, f'secret leaked in {path.name}'
print(json.dumps(verification,ensure_ascii=False));print('Artifact secret scan PASS')
