{
  "workspaces": [
    "packages/*"
  ],
  "name": "gajae-way",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "author": "Yeachan Heo",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "Gajae Way operator-agent runtime",
  "scripts": {
    "test": "bun test packages",
    "build": "bun build --compile packages/gateway/src/main.ts --outfile dist/gajaeway-gateway && bun build --compile packages/adapter-discord/src/main.ts --outfile dist/gajaeway-discord && bun build --compile packages/adapter-telegram/src/main.ts --outfile dist/gajaeway-telegram && bun build --compile packages/admin/src/main.ts --outfile dist/gajaeway-admin && bun build --compile packages/cli/src/main.ts --outfile dist/gajaeway"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
